class DisplayData {

    count = 0;
    placeNames = []; // should add name attribute to the tables resto, activity and culture...

    constructor(fetchPHP, ) {
      // Constructor code goes here, if needed
    }
  
    fetchData(tableName) {
      // Method for fetching data goes here
    }
  
    // Add more methods as needed
  }
  
  // Example usage:
  // const displayDataInstance = new DisplayData();
  // displayDataInstance.fetchData('restaurant');
  